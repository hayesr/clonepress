<!DOCTYPE html>
<html>
<head>
  <title>Clonepress</title>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
</head>
<body>

  <% if flash.any? %>
  <div class="messages">
    <% flash.each do |k,v| %>
    <div class="<%= k %>">
      <%= v %>
    </div>
    <% end %>
  </div>
  <% end %>
<%= yield %>

</body>
</html>
